use gtk::prelude::*;
use std::io::prelude::*;
use gtk::{Button, Window, WindowType, FileChooserDialog, FileChooserAction, ResponseType};
use std::fs;

pub mod circuit_file_reader {
    pub fn load_file()
    {
    }
}
